[package]
name = "brarchive-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
brarchive = { path = "../brarchive" }

log = "0.4"
fern = { version = "0.7", features = ["colored"] }
chrono = "0.4"

humantime = "2.1"
